name: Api-Gateway github action

on:
  push:
    branches:
      - main

jobs:
  api-gateway:
    runs-on: ubuntu-latest
    container:
      image: maven:latest
    steps:
       - name: Run Maven clean install
         run: mvn clean install
       - name: open-jdk install
         run: openjdk:24-jdk-slim:latest
